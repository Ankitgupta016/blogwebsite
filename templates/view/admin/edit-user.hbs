<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta property="image" content="img/apple-touch-icon.png" />
    <meta name="author" content="Ankit gupta" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#000000" />
  <title>WanderWords-food,health,travel,share market and meny more blog
    </title>
    <link rel="icon" type="image/x-icon" href="img/favicon.ico" />
    <link href="../css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="css/style.css" />

    <style>
      html { height: 100vh; } input, label { font-weight: 600; border-radius:
      4px; font-size: 14px; padding-left: 27px !important; padding: 10px 20px;
      appearance: textfield; outline: none; color: #111112; border: 1px solid
      wheat; margin: 10px 0; } input[type="submit"] { border: 1px solid rgb(169,
      168, 167); background-color: rgb(169, 168, 167); }
      input[type="submit"]:hover { border: 1px solid rgb(169, 168, 167);
      background-color: rgb(240, 239, 238); } input[type="radio"] { height: 19px
      !important; width: 19px !important; } form { width: 40%; } @media
      (max-width:885px) and (min-width: 467px) { form { width: 70%; } } @media
      (max-width:466px) and (min-width: 200px) { form { width: 99%; } }
    </style>
  </head>

  <body style="height: 100vh;" class="d-flex align-items-center">
    <div class="container-fluid d-flex justify-content-center">
      <form
        method="post"
        class="d-flex justify-content-center flex-column m-auto"
      >
        <input type="hidden" name="user_id" value="{{user._id}}" />

        <input
          type="text"
          name="name"
          value="{{user.name}}"
          placeholder="enter name"
        />

        <input
          type="text"
          name="email"
          value="{{user.email}}"
          placeholder="enter email"
        />

        <input
          type="text"
          name="number"
          value="{{user.number}}"
          placeholder="enter number"
        />

        <div class="d-flex align-items-center justify-content-between">
          <label>Verified</label>
          <input type="radio" name="is_verified" value="true" {{#if user.is_verified}}checked{{/if}}>
          <label>Unverified</label>
          <input type="radio" name="is_verified" 
          value="false" {{#unless user.is_verified}}checked{{/unless}}>

        </div>

        <input type="submit" value="Update user" />
      </form>
    </div>

  </body>

</html>